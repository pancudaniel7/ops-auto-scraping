version: "3.8"
services:
  rabbitmq:
    image: rabbitmq:3.9.11-management-alpine
    container_name: 'rabbitmq'
    mem_limit: 6000m
    ports:
        - 5672:5672
        - 15672:15672
    volumes:
        - ~/.docker-conf/rabbitmq/data/:/tmp/lib/rabbitmq/
        - ~/.docker-conf/rabbitmq/log/:/tmp/log/rabbitmq
        - ./config/definitions.file.json:/tmp/definitions.file.json
    networks:
        - auto_scraping
    environment:
        - RABBITMQ_SERVER_ADDITIONAL_ERL_ARGS=-rabbitmq_management load_definitions "/tmp/definitions.file.json"
        
networks:
  auto_scraping:
    driver: bridge
